import { themeSizes } from './size';

export const darkTheme = {
  ...themeSizes,
  primaryColor: '#333',
  secondaryColor: '#666',
  color: '#fff',
  boxShadow: '0px 0px 5px rgba(0, 0, 0, 0.2)',
  background: '#131313',
};
